<%= form_for(@proteam) do |f| %>
  <% if @proteam.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@proteam.errors.count, "error") %> prohibited this proteam from being saved:</h2>

      <ul>
      <% @proteam.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
